{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Local Mock Stack",
            "type": "shell",
            "command": "bash ${workspaceFolder}/dev/start-local.sh",
            "isBackground": true,
            "problemMatcher": {
                "pattern": {
                    "regexp": "^$"
                },
                "background": {
                    "activeOnStart": true,
                    "beginsPattern": "Local mock stack started",
                    "endsPattern": "Frontend:"
                }
            },
            "presentation": {
                "reveal": "always",
                "panel": "dedicated",
                "group": "server"
            }
        },
        {
            "label": "Start PHP Server",
            "type": "shell",
            "command": "php -S localhost:8080 -t ${workspaceFolder}/public",
            "isBackground": true,
            "problemMatcher": {
                "pattern": {
                    "regexp": "^$"
                },
                "background": {
                    "activeOnStart": true,
                    "beginsPattern": "PHP .* Development Server",
                    "endsPattern": "started"
                }
            },
            "presentation": {
                "reveal": "always",
                "panel": "dedicated",
                "group": "server"
            }
        },
        {
            "label": "Test: Connection-Check (curl)",
            "type": "shell",
            "command": "curl -s -X POST http://localhost:8080/webhook.php -H 'Content-Type: application/json' -H 'Authorization: Bearer ${input:bearerToken}' -H 'X-Test-Connection: true' -d '{}' | python3 -m json.tool",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        },
        {
            "label": "Test: OTLP Span senden (curl)",
            "type": "shell",
            "command": "curl -s -X POST http://localhost:8080/webhook.php -H 'Content-Type: application/json' -H 'Authorization: Bearer ${input:bearerToken}' -d @${workspaceFolder}/.vscode/test-payload.json | python3 -m json.tool",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ],
    "inputs": [
        {
            "id": "bearerToken",
            "type": "promptString",
            "description": "Bearer Token aus config.php",
            "default": "PASTE_YOUR_BEARER_TOKEN_HERE"
        }
    ]
}
